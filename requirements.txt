# Core scientific computing packages
numpy>=1.19.0
pandas>=1.2.0
matplotlib>=3.3.0
seaborn>=0.11.0
scipy>=1.6.0

# Astronomy packages
astropy>=4.2
astroquery>=0.4.0

# Web and SSL handling
requests>=2.25.0
certifi
urllib3>=1.26.0

# Optional but recommended for data handling
pyarrow  # For Parquet file support
tables   # For HDF5 file support

# Optional for better performance
numexpr  # For faster pandas operations
bottleneck  # For faster NaN handling

# SSL and system-specific (comment out if causing issues)
# pywin32  # Windows only - uncomment if on Windows
# keyring  # For secure credential storage